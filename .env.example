# Supabase Configuration
# These can be found in your Supabase project settings under API.
SUPABASE_URL="your-supabase-url"
SUPABASE_ANON_KEY="your-supabase-anon-key"

# Supabase Database Connection URL (for Drizzle ORM)
# This can be found in your Supabase project settings under Database > Connection string (URI).
# Make sure to replace [YOUR-PASSWORD] with your actual database password.
SUPABASE_DB_URL="postgresql://postgres:[YOUR-PASSWORD]@db.your-supabase-project-id.supabase.co:5432/postgres"

# Allora Network Configuration (using SDK v2)
# API Key for accessing the Allora API. Obtain from the Allora team.
ALLORA_API_KEY="your-allora-api-key"
# Chain slug for the Allora network (e.g., 'testnet', 'mainnet').
ALLORA_CHAIN_SLUG="testnet"


# CoinGecko API Key (Optional but recommended for higher rate limits)
# You can get an API key from CoinGecko's website.
# If you don't have one, some public endpoints might work but with stricter rate limits.
COINGECKO_API_KEY="your-coingecko-api-key"

# Cron Job Secret
# A secure, randomly generated string to protect your cron job endpoints.
# This secret will be checked by your cron job handlers to ensure requests are legitimate.
CRON_SECRET="generate-a-strong-random-secret-here"

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000

# Next.js Public Environment Variables (if any, prefix with NEXT_PUBLIC_)
# Example: NEXT_PUBLIC_POSTHOG_KEY="your-posthog-key"
# Example: NEXT_PUBLIC_POSTHOG_HOST="your-posthog-host"